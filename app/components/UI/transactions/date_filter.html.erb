<%= render UI::YearMonthFilter.new(
      years: years,
      selected_year: selected_year,
      selected_month: selected_month,
      href_for: ->(year, month) { href_for(year: year, month: month) },
      clear_href: clear_href,
      label: custom_label,
      active: active?
    ) %>
